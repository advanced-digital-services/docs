---
title: 'Quickstart'
description: 'Signup, autenticação e primeira transação SPEI'
---

## Pré-requisitos

- `clientId` e `clientSecret` (obtidos via `POST /api/signup` ou no painel)
- Certificado X.509 (mTLS) fornecido pelo NTX Pay
- URL HTTPS para receber webhooks

## 1. Criar conta

```bash
curl -X POST https://sandbox.ntxpay.com/api/signup \
  -H "Content-Type: application/json" \
  -d '{
    "holderName": "Maria Lopez",
    "holderTaxIdPrimary": "LOMA850101ABC",
    "holderTaxIdPrimaryType": "RFC",
    "accountType": "PF",
    "email": "maria@example.com",
    "isSandbox": true
  }'
```

A resposta traz `credentials.clientId` e `credentials.clientSecret`. O `clientSecret` é exibido uma única vez.

## 2. Gerar token

```bash
ENCODED_CERT=$(cat client.cert.pem | python3 -c "import sys,urllib.parse; print(urllib.parse.quote(sys.stdin.read()))")

curl -X POST https://sandbox.ntxpay.com/api/auth/token \
  -H "X-SSL-Client-Cert: $ENCODED_CERT" \
  -H "Content-Type: application/json" \
  -d '{"clientId":"qr-93-550e8400","clientSecret":"a1b2..."}'
```

Retorna `access_token` com validade de 10 minutos.

## 3. Configurar webhook

```bash
curl -X POST https://sandbox.ntxpay.com/api/webhooks-config \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://meu-servidor.com/webhooks/ntxpay",
    "events": ["cash_in", "cash_out"]
  }'
```

A resposta traz o `secret` usado para validar a assinatura HMAC.

## 4. Criar cobrança SPEI

```bash
curl -X POST https://sandbox.ntxpay.com/api/spei/cash-in \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "amountCentavos": 50000,
    "externalId": "order-001",
    "customerName": "Juan Perez"
  }'
```

Retorna `destinationClabe`. O pagador transfere para essa CLABE; a confirmação chega via webhook `cash_in`.

## Ambientes

| Ambiente | URL | Provider |
|---|---|---|
| Sandbox | `https://sandbox.ntxpay.com` | `sandbox` (transações simuladas) |
| Produção | `https://sandbox.ntxpay.com` | `smartfastpay` |

Conta sandbox: `isSandbox: true` no signup.
